@using MedicoApp.Core.Application.ViewModels.User;
@model SaveUserViewModel

@{
    ViewData["Title"] = "Delete";
}

<div class="row">

    <div class="col-6 offset-3" >
        <div class="card">
            <div class="card-header bg-danger text-light">
                <h4>Esta seguro que desea eliminar este Usuario?</h4>
            </div>
            <div class="card-body">

                <form method="post" asp-controller="User" asp-action="DeletePost">

                    <input type="hidden" asp-for="Id" />

                    <button type="submit" class="btn btn-danger">Eliminar</button>
                    <a asp-controller="User" asp-action="MantUser" class="btn btn-warning">Cancelar</a>

                </form>

            </div>
        </div>
    </div>

</div>
